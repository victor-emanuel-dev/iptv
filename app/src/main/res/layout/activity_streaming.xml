<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:background="@color/player_background">

    <!-- Video Player - Lado Esquerdo (60%) -->
    <FrameLayout
        android:id="@+id/playerContainer"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="60"
        android:background="@color/player_background"
        android:focusable="true"
        android:focusableInTouchMode="true">

        <androidx.media3.ui.PlayerView
            android:id="@+id/playerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            app:use_controller="false"
            app:show_buffering="when_playing"
            app:surface_type="texture_view"
            app:resize_mode="fill" />

        <!-- Controles overlay -->
        <LinearLayout
            android:id="@+id/playerOverlay"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:background="@color/overlay_background"
            android:alpha="0"
            android:visibility="gone">

            <!-- Top controls -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="end"
                android:padding="8dp">

                <ImageButton
                    android:id="@+id/btnSkipChannel"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:background="@drawable/circle_background_dark"
                    android:src="@drawable/ic_skip_next"
                    android:contentDescription="Próximo Canal"
                    android:scaleType="center"
                    android:layout_marginEnd="8dp"
                    android:focusable="true"
                    android:nextFocusDown="@id/btnPlayPause"
                    android:nextFocusLeft="@id/btnFullScreen" />

                <ImageButton
                    android:id="@+id/btnFullScreen"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:background="@drawable/circle_background_dark"
                    android:src="@drawable/ic_fullscreen"
                    android:contentDescription="Tela Cheia"
                    android:scaleType="center"
                    android:layout_marginEnd="8dp"
                    android:focusable="true"
                    android:nextFocusDown="@id/btnPlayPause"
                    android:nextFocusRight="@id/btnSkipChannel" />
            </LinearLayout>

            <View
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1" />

            <!-- Bottom controls -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center"
                android:padding="8dp">

                <ImageButton
                    android:id="@+id/btnPlayPause"
                    android:layout_width="56dp"
                    android:layout_height="56dp"
                    android:background="@drawable/circle_background_white"
                    android:src="@drawable/ic_pause"
                    android:contentDescription="Play/Pause"
                    android:scaleType="center"
                    android:layout_marginEnd="16dp"
                    android:focusable="true"
                    android:nextFocusUp="@id/btnFullScreen"
                    android:nextFocusRight="@id/btnMute" />

                <ImageButton
                    android:id="@+id/btnMute"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:background="@drawable/circle_background_dark"
                    android:src="@drawable/ic_volume_up"
                    android:contentDescription="Mudo"
                    android:scaleType="center"
                    android:focusable="true"
                    android:nextFocusUp="@id/btnFullScreen"
                    android:nextFocusLeft="@id/btnPlayPause" />
            </LinearLayout>
        </LinearLayout>

        <!-- Progress indicator -->
        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:layout_gravity="center"
            android:indeterminateTint="@color/white"
            android:visibility="gone" />
    </FrameLayout>

    <!-- Painel Lateral - Lado Direito (40%) - Lista de Canais - SEM PADDING DIREITO -->
    <LinearLayout
        android:id="@+id/sidePanel"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="40"
        android:orientation="vertical"
        android:background="@color/panel_background"
        android:paddingStart="0dp"
        android:paddingTop="0dp"
        android:paddingEnd="0dp"
        android:paddingBottom="0dp">

        <!-- Header com logo e navigation -->
        <LinearLayout
            android:id="@+id/sidePanelHeader"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@color/surface_dark"
            android:paddingLeft="8dp"
            android:paddingRight="8dp"
            android:paddingVertical="8dp">

            <!-- Logo e nome COM ENGRENAGEM -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <ImageView
                    android:id="@+id/ivAppLogo"
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:src="@drawable/logo"
                    android:contentDescription="@string/app_logo"
                    android:scaleType="fitCenter"
                    android:layout_marginStart="8dp" />

                <TextView
                    android:id="@+id/tvAppName"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:layout_marginStart="8dp"
                    android:text="TubiTV Premium"
                    android:textStyle="bold"
                    android:textColor="@color/white"
                    android:textSize="16sp"
                    android:maxLines="1" />

                <!-- ENGRENAGEM DE CONFIGURAÇÕES -->
                <ImageButton
                    android:id="@+id/btnAdminAccess"
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:src="@drawable/ic_settings"
                    android:contentDescription="Configurações Admin"
                    android:scaleType="center"
                    android:layout_marginStart="8dp"
                    android:layout_marginEnd="8dp"
                    android:tint="@color/primary_color"
                    android:focusable="true"
                    android:nextFocusDown="@id/tabChannels" />
            </LinearLayout>

            <!-- Tabs de navegação -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:layout_marginTop="8dp">

                <TextView
                    android:id="@+id/tabChannels"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Canais"
                    android:textColor="@color/white"
                    android:textStyle="bold"
                    android:textSize="14sp"
                    android:gravity="center"
                    android:padding="8dp"
                    android:background="@color/primary_color"
                    android:focusable="true"
                    android:nextFocusUp="@id/btnAdminAccess"
                    android:nextFocusDown="@id/btnPremiumBanner"
                    android:nextFocusRight="@id/tabInfo" />

                <TextView
                    android:id="@+id/tabInfo"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Info"
                    android:textColor="@color/text_secondary"
                    android:textSize="14sp"
                    android:gravity="center"
                    android:padding="8dp"
                    android:background="@color/button_background"
                    android:layout_marginStart="2dp"
                    android:focusable="true"
                    android:nextFocusUp="@id/btnAdminAccess"
                    android:nextFocusDown="@id/btnPremiumBanner"
                    android:nextFocusLeft="@id/tabChannels" />
            </LinearLayout>
        </LinearLayout>

        <!-- Conteúdo principal -->
        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">

            <!-- SEÇÃO DE CANAIS -->
            <LinearLayout
                android:id="@+id/channelsSection"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:orientation="vertical"
                android:visibility="visible">

                <!-- ========== BOTÃO PREMIUM DOURADO ========== -->
                <Button
                    android:id="@+id/btnPremiumBanner"
                    android:layout_width="match_parent"
                    android:layout_height="70dp"
                    android:text="👑 ACESSO PREMIUM ⭐⭐⭐⭐⭐"
                    android:textColor="@color/black"
                    android:textStyle="bold"
                    android:textSize="18sp"
                    android:backgroundTint="@color/gold"
                    android:layout_margin="0dp"
                    android:focusable="true"
                    android:nextFocusUp="@id/tabChannels"
                    android:nextFocusDown="@id/recyclerChannels" />

                <!-- Lista de Canais - SEM PADDING DIREITO -->
                <androidx.recyclerview.widget.RecyclerView
                    android:id="@+id/recyclerChannels"
                    android:layout_width="match_parent"
                    android:layout_height="0dp"
                    android:layout_weight="1"
                    android:paddingStart="4dp"
                    android:paddingTop="4dp"
                    android:paddingEnd="0dp"
                    android:paddingBottom="4dp"
                    android:clipToPadding="false"
                    android:focusable="true"
                    android:nextFocusUp="@id/btnPremiumBanner"
                    android:nextFocusLeft="@id/btnPlayPause" />
            </LinearLayout>

            <!-- Painel de Informações - AJUSTADO PARA OCUPAR TODA A LARGURA -->
            <ScrollView
                android:id="@+id/infoPanel"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:visibility="gone"
                android:padding="16dp"
                android:background="@color/panel_background">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical">

                    <!-- Canal Atual - OCUPANDO TODA A LARGURA COM MARGEM ADEQUADA -->
                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="vertical"
                        android:background="@color/card_background"
                        android:padding="16dp"
                        android:layout_marginBottom="16dp">

                        <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="CANAL ATUAL"
                            android:textColor="@color/text_secondary"
                            android:textSize="10sp"
                            android:textStyle="bold"
                            android:layout_marginBottom="4dp" />

                        <TextView
                            android:id="@+id/tvChannelName"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:textStyle="bold"
                            android:textSize="18sp"
                            android:textColor="@color/white"
                            android:layout_marginBottom="4dp" />

                        <TextView
                            android:id="@+id/tvChannelDescription"
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:textColor="@color/text_secondary"
                            android:textSize="12sp"
                            android:layout_marginBottom="8dp" />

                        <!-- Status ao vivo -->
                        <LinearLayout
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:orientation="horizontal"
                            android:gravity="center_vertical"
                            android:background="@color/background_dark"
                            android:paddingHorizontal="8dp"
                            android:paddingVertical="4dp">

                            <View
                                android:layout_width="6dp"
                                android:layout_height="6dp"
                                android:background="@color/error_color"
                                android:layout_marginEnd="6dp" />

                            <TextView
                                android:id="@+id/tvStatus"
                                android:layout_width="wrap_content"
                                android:layout_height="wrap_content"
                                android:text="🔄 Carregando..."
                                android:textColor="@color/error_color"
                                android:textStyle="bold"
                                android:textSize="10sp" />
                        </LinearLayout>
                    </LinearLayout>

                    <!-- Estatísticas - OCUPANDO TODA A LARGURA COM MARGEM ADEQUADA -->
                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="vertical"
                        android:background="@color/background_dark"
                        android:padding="16dp">

                        <TextView
                            android:layout_width="wrap_content"
                            android:layout_height="wrap_content"
                            android:text="TRANSMISSÃO"
                            android:textColor="@color/text_secondary"
                            android:textSize="10sp"
                            android:textStyle="bold"
                            android:layout_marginBottom="8dp" />

                        <TextView
                            android:layout_width="match_parent"
                            android:layout_height="wrap_content"
                            android:text="Qualidade: Automática\nProtocolo: HLS/HTTP\nBuffer: Adaptativo"
                            android:textColor="@color/text_secondary"
                            android:textSize="12sp"
                            android:lineSpacingExtra="2dp" />
                    </LinearLayout>
                </LinearLayout>
            </ScrollView>
        </FrameLayout>
    </LinearLayout>

</LinearLayout>